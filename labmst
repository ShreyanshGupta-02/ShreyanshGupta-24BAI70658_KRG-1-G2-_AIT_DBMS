CREATE TABLE Employees (
    EmpID INT PRIMARY KEY,
    EmpName VARCHAR(50),
    Salary INT
);
INSERT INTO Employees (EmpID, EmpName, Salary) VALUES
(1, 'Asha', 50000),
(2, 'Rohan',  70000),
(3, 'Meera',  40000),
(4, 'Kabir', 55000),
(5, 'Isha', 90000);
CREATE VIEW HighSalaryEmployess AS
SELECT EmpID,EmpName,Salary
FROM Employees
WHERE Salary > 50000;
